#!/usr/bin/env bash

. "$HOME"/dotfiles/bin/dwm/colorscheme

case "$BLOCK_BUTTON" in
    1 | 3) "$HOME"/dotfiles/bin/change_language.sh ;;
esac

notify-send "button -- $BLOCK_BUTTON"
# case "$BLOCK_BUTTON" in
#     1) notify-send "button 1" ;;
#     2) notify-send "button 2" ;;
#     3) notify-send "button 3" ;;
#     4) notify-send "button 4" ;;
#     5) notify-send "button 5" ;;
#     6) notify-send "button 6" ;;
#     *) notify-send "button -- $BLOCK_BUTTON" ;;
# esac

declare -A lmap
lmap["ua"]="UA"
lmap["us"]="US"

k_layout_icon=" "
k_layout=$(setxkbmap -query | grep layout | awk '{print $NF}')

printf "^c#8caaee^ $k_layout_icon ^c#cba6f7^${lmap[$k_layout]}^d^$SPLIT"