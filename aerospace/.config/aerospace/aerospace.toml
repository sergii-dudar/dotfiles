# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# Config version for compatibility and deprecations
# See: https://nikitabobko.github.io/AeroSpace/guide#config-version
# Fallback value (if you omit the key): config-version = 1
config-version = 2

# You can use it to add commands that run after AeroSpace startup.
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# Start AeroSpace at login
start-at-login = true

# Automatically reload the config when the config file is saved
# After setting this to true, reload once manually to start the auto-reloading
auto-reload-config = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 15

# Possible values: tiles|accordion
default-root-container-layout = 'accordion'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Mouse follows focus when focused window or workspace changes
on-focus-changed = "move-mouse window-lazy-center"

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = false

# List of workspaces that should stay alive even when they contain no windows,
# even when they are invisible.
# This config option is only available since 'config-version = 2'
# Fallback value (if you omit the key): persistent-workspaces = []
persistent-workspaces = ["1", "2", "3", "4", "5", "6", "7", "8", "9"]

# A callback that runs every time binding mode changes
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# See: https://nikitabobko.github.io/AeroSpace/commands#mode
on-mode-changed = []

focus-follows-mouse.enabled = false

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
gaps.inner.horizontal = 0
gaps.inner.vertical =   0
gaps.outer.left =       0
gaps.outer.bottom =     0
gaps.outer.top =        0
gaps.outer.right =      0

# Local gap presets
# gaps.inner.horizontal = 5
# gaps.inner.vertical = 5
# gaps.outer.left = 3
# gaps.outer.right = 3
# gaps.outer.bottom = 2

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

    # All possible keys:
    # - Letters.        a, b, c, ..., z
    # - Numbers.        0, 1, 2, ..., 9
    # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
    # - F-keys.         f1, f2, ..., f20
    # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
    #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
    #                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
    #                   sectionSign (ISO keyboards only, european keyboards only)
    # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
    #                   keypadMinus, keypadMultiply, keypadPlus
    # - Arrows.         left, down, up, right

    # All possible modifiers: cmd, alt, ctrl, shift

    # All possible commands: https://nikitabobko.github.io/AeroSpace/commands

    # See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
    # You can uncomment the following lines to open up terminal with alt + enter shortcut
    # (like in i3)
    # alt-enter = '''exec-and-forget osascript -e '
    # tell application "Terminal"
    #     do script
    #     activate
    # end tell'
    # '''

    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    # alt-slash = 'layout tiles horizontal vertical'
    # alt-comma = 'layout accordion horizontal vertical'
    alt-s = 'layout v_accordion' # 'layout stacking' in i3
    alt-w = 'layout h_accordion' # 'layout tabbed' in i3
    alt-e = 'layout tiles horizontal vertical' # 'layout toggle split' in i3
    alt-shift-e = 'layout floating tiling' # 'floating toggle' in i3

    # See: https://nikitabobko.github.io/AeroSpace/commands#focus
    cmd-h = 'focus left'
    cmd-j = 'focus down'
    cmd-k = 'focus up'
    cmd-l = 'focus right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move
    cmd-shift-h = 'move left'
    cmd-shift-j = 'move down'
    cmd-shift-k = 'move up'
    cmd-shift-l = 'move right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#resize
    # alt-minus = 'resize smart -50'
    # alt-equal = 'resize smart +50'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
    alt-1 = 'workspace 1'
    alt-2 = 'workspace 2'
    alt-3 = 'workspace 3'
    alt-4 = 'workspace 4'
    alt-5 = 'workspace 5'
    alt-6 = 'workspace 6'
    alt-7 = 'workspace 7'
    alt-8 = 'workspace 8'
    alt-9 = 'workspace 9'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
    # Move window to another ws, and focus that ws
    alt-shift-1 = ['move-node-to-workspace 1', 'workspace 1']
    alt-shift-2 = ['move-node-to-workspace 2', 'workspace 2']
    alt-shift-3 = ['move-node-to-workspace 3', 'workspace 3']
    alt-shift-4 = ['move-node-to-workspace 4', 'workspace 4']
    alt-shift-5 = ['move-node-to-workspace 5', 'workspace 5']
    alt-shift-6 = ['move-node-to-workspace 6', 'workspace 6']
    alt-shift-7 = ['move-node-to-workspace 7', 'workspace 7']
    alt-shift-8 = ['move-node-to-workspace 8', 'workspace 8']
    alt-shift-9 = ['move-node-to-workspace 9', 'workspace 9']

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
    alt-tab = 'focus-back-and-forth || workspace-back-and-forth'
    # See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
    alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

    # See: https://nikitabobko.github.io/AeroSpace/commands#fullscreen
    alt-shift-h = 'macos-native-minimize'
    # alt-shift-f = 'macos-native-fullscreen'
    alt-f = 'fullscreen --no-outer-gaps'

    # See: https://nikitabobko.github.io/AeroSpace/commands#focus-monitor
    alt-comma = 'focus-monitor --wrap-around prev'
    alt-period = 'focus-monitor --wrap-around next'

    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    alt-shift-semicolon = 'mode service'
    alt-shift-r = 'mode resize'
    # alt- = 'mode scratchpad'

    ################################################################
    # ------------------------- Scratchpads -----------------------#
    ################################################################

    cmd-shift-y = 'exec-and-forget ~/.config/aerospace/scratchpad.sh com.googlecode.iterm2 iTerm iTerm'
    cmd-shift-g = 'exec-and-forget ~/.config/aerospace/scratchpad.sh com.brave.Browser.app.pommaclcbfghclhalboakcipcmmndhcj "Google Chat" "Google Chat 1"'
    cmd-shift-s = 'exec-and-forget ~/.config/aerospace/scratchpad.sh com.sublimetext.4 "Sublime Text" "Sublime Text"'
    cmd-shift-u = 'exec-and-forget ~/.config/aerospace/scratchpad.sh com.brave.Browser.app.picebhhlijnlefeleilfbanaghjlkkna "Monkeytype" "Monkeytype"'
    # cmd-shift-m = 'exec-and-forget ~/.config/aerospace/scratchpad.sh com.apple.Music Music Music'

    # Quick navigation
    cmd-b = 'exec-and-forget open -a Brave\ Browser'
    cmd-i = 'exec-and-forget open -a IntelliJ\ IDEA'
    # cmd-t = 'exec-and-forget open -a Ghostty'
    cmd-t = 'exec-and-forget open -a Alacritty'

    # TODO:
    cmd-ctrl-r = 'exec-and-forget ~/.config/aerospace/scratchpad_hideall.sh "iTerm" "Google Chat" "Sublime Text" "Music"'

################################################################
# -------------------------- Modes-----------------------------#
################################################################

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # reset layout
    f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
    backspace = ['close-all-windows-but-current', 'mode main']

    # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
    #s = ['layout sticky tiling', 'mode main']

    alt-shift-h = ['join-with left', 'mode main']
    alt-shift-j = ['join-with down', 'mode main']
    alt-shift-k = ['join-with up', 'mode main']
    alt-shift-l = ['join-with right', 'mode main']

[mode.resize.binding]
    h = 'resize width -50'
    j = 'resize height +50'
    k = 'resize height -50'
    l = 'resize width +50'
    b = 'balance-sizes'

    # See: https://nikitabobko.github.io/AeroSpace/commands#resize
    minus = 'resize smart -50'
    equal = 'resize smart +50'
    enter = 'mode main'
    esc = 'mode main'

# second monitor configuration: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[workspace-to-monitor-force-assignment]
    9 = 'secondary'

################################################################
# ------------------------- Roles -----------------------------#
################################################################
# command to get application id: osascript -e 'id of app "Google Chat"'
# aerospace list-apps

[[on-window-detected]]
    if = 'test %{app-bundle-id} = net.kovidgoyal.kitty'
    run = "move-node-to-workspace 1"

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.mitchellh.ghostty'
    run = "move-node-to-workspace 1"

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.github.wez.wezterm'
    run = "move-node-to-workspace 1"

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.jetbrains.intellij'
    run = "move-node-to-workspace 1"

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.jetbrains.intellij.ce'
    run = "move-node-to-workspace 1"

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.microsoft.VSCode'
    run = "move-node-to-workspace 1"

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.google.Chrome'
    run = "move-node-to-workspace 2"

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.brave.Browser'
    run = "move-node-to-workspace 2"

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.microsoft.teams2'
    run = "move-node-to-workspace 9"

# Force tile all the windows (or windows of a particular app)
# [[on-window-detected]]
#     if = 'test %{app-bundle-id} = ...'
#     run = 'layout tiling'

# Force float all the windows (or windows of a particular app)
[[on-window-detected]]
    # if = 'test %{app-bundle-id} = com.google.Chrome.app.mdpkiolbdkhdjpekfbkbmhigcaggjagi' # Goodle Chat
    if = 'test %{app-bundle-id} = com.brave.Browser.app.pommaclcbfghclhalboakcipcmmndhcj' # Goodle Chat
    run = 'layout floating'
    # run = ['layout floating', 'move-node-to-workspace 9']

[[on-window-detected]]
    # if = 'test %{app-bundle-id} = com.google.Chrome.app.picebhhlijnlefeleilfbanaghjlkkna' # Monkeytype
    if = 'test %{app-bundle-id} = com.brave.Browser.app.picebhhlijnlefeleilfbanaghjlkkna' # Monkeytype
    run = 'layout floating'

# [[on-window-detected]]
#     if = 'test %{app-bundle-id} = com.brave.Browser.app.gpkmanblioapcnifgdalafnclogeoebh' # VimHero
#     run = 'layout floating'
#
# [[on-window-detected]]
#     # if = 'test %{app-bundle-id} = com.google.Chrome.app.hklfmmehghkmgpoombkhphgoebceokhg' # Text Compare
#     if = 'test %{app-bundle-id} = com.brave.Browser.app.hklfmmehghkmgpoombkhphgoebceokhg'
#     run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.iCal'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.finder'
    run = 'layout floating'

# [[on-window-detected]]
#     if = 'test %{app-bundle-id} = com.sublimetext.4'
#     #run = "move-node-to-workspace 3"
#     run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.googlecode.iterm2'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.ActivityMonitor'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.clock'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.Automator'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.ScriptEditor2'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.weather'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.Music'
    # run = 'layout floating'
    run = "move-node-to-workspace 9"